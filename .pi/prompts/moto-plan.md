---
description: Create an implementation plan in docs/plans/ from a spec
argument-hint: "<spec-id or feature>"
---

Load the `moto-gt-docs` skill.

Find or read the spec for ${1}. Create a corresponding plan in `docs/plans/` from `docs/plans/TEMPLATE.md`, linking back to the spec and any relevant ADRs.

If the spec is not approved yet, mark the plan as `draft` and note that the spec needs review first.

Context: $@