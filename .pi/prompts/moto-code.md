---
description: Delegate a MotoGT coding task to the project coding agent
argument-hint: "<task>"
---

Use the `subagent` tool with `agentScope: "both"` and `confirmProjectAgents: false` to delegate this coding task to `kimi-coder`.

Use `moto-gt-coding` skill reference if the task involves Phaser scenes, race simulation, or project architecture.

Task: $@