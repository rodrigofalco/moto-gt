---
description: Delegate high-level design/planning to glm-planner
argument-hint: "<task>"
---

Use the `subagent` tool with `agentScope: "both"` and `confirmProjectAgents: false` to delegate this design task to `glm-planner`.

After the plan is returned, ask if I want to turn it into a spec/plan document or hand it off to `kimi-coder` for implementation.

Task: $@