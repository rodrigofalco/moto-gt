---
description: Draft a MotoGT feature spec in docs/spec/
argument-hint: "<feature description>"
---

Load the `moto-gt-docs` skill and use the `moto-gt-coding` skill for technical context.

Create a new spec in `docs/spec/` from `docs/spec/TEMPLATE.md`. Pick the next sequential spec ID.

Feature to specify: $@